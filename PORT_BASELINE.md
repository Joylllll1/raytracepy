# RayTracePy 鸿蒙 PC 移植基线

## 1. 基线信息

| 项目 | 基线 |
|---|---|
| 上游项目 | `raytracepy` |
| PyPI 版本 | `0.0.1` |
| 源码提交 | `e900628754258bcf86099cc3821f8e9155b7b5a7` |
| Python 要求 | Python 3.10 或更高（本项目统一使用 Python 3.10） |
| 许可证 | BSD 3-Clause |
| 集成分支 | `port/harmonyos-pc` |
| 目标平台 | 鸿蒙 PC 原生环境 |

基线提交取自当前 Fork 的 `master` 分支。所有鸿蒙移植工作以该提交为起点，功能分支通过 PR 合并到 `port/harmonyos-pc`，不直接修改 `master`。

## 2. 项目范围

本轮移植包含：

- Python 库的安装与导入；
- 命令行计算；
- 至少一个官方示例；
- 参考环境与鸿蒙 PC 的结果对比；
- 可复现的安装、测试和交付文档。

本轮移植不包含：

- 桌面 GUI；
- 从头移植 CPython、NumPy、SciPy 或 LLVM；
- 虚拟机、容器或兼容层方案，除非老师明确同意调整范围。

## 3. 依赖基线

仓库 `requirements.txt` 当前固定版本如下：

```text
numpy==1.22.0
numba~=0.53.1
plotly==5.6.0
scipy==1.10.0
pandas==1.4.1
datashader==0.13.0
```

鸿蒙端最终可用版本由依赖适配负责人验证后写入 `requirements-harmonyos.txt`，不得在验证前假定上述版本均可安装。

## 4. 验收示例

首选官方示例：`examples/single/single_light.py`。

基准与测试负责人需要固定随机性、输入参数和输出指标，并准备规模较小的自动化测试；最终演示可以使用完整参数运行。若该示例受绘图库或性能限制阻塞，应在进度记录中说明原因并由组长确认替代示例。

## 5. 验收标准

- [ ] 鸿蒙 PC 上存在可用的原生 Python 3.10 环境；
- [ ] 可以安装 RayTracePy 及必要依赖；
- [ ] `import raytracepy` 成功；
- [ ] 至少一个官方示例完整运行；
- [ ] 固定输入下的计算结果与参考环境在约定误差内一致；
- [ ] 在全新环境中可按照文档重新安装并复现结果；
- [ ] 测试、文档、安装产物和已知问题记录齐全。

## 6. 已知风险

- 仓库包含 `src/raytracepy/compile/math_custom.cp310-win_amd64.pyd`，该文件仅适用于 Windows AMD64。当前常规 `raytracepy` 导入路径未直接引用它，但源码适配负责人仍需确认鸿蒙端不依赖该文件。
- `numba` 及其 LLVM 运行时可能是鸿蒙端的关键阻塞项，需要优先验证。
- 绘图依赖可能无法完整安装；应将核心数值计算和可视化分别验收，避免绘图问题掩盖核心功能结果。

## 7. 第 5 天决策门

第 5 天由组长根据 Python 环境、关键依赖、导入测试和最小示例结果作出结论：

1. 条件满足：继续完整移植和集成；
2. 条件不满足：整理最小原型、错误日志和依赖阻塞报告，向老师申请调整验收范围。

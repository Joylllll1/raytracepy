# 未解决依赖与错误日志（4号）

## numba JIT 不可用

- 现象：import / 编译 OK，执行编译产物 → 段错误（`si_addr=0x3c091000500`）
- 已排除：numba 0.60/0.61 × llvmlite 0.43/0.44 × LLVM 14/15，均复现
- 规避：纯 Python 回退（`numba.njit` → 恒等装饰器），**源码改动归 5号**
- 影响：约 2.9× 慢；数值与 Windows 参考哈希一致

## 解释器验收未决

- `~/.local/alpine-py310` 为 Alpine 构建，非为鸿蒙编译（原生执行）
- 若要求「为鸿蒙编译」→ 改用 `python@3.12` 并重测全部依赖

## 其他

- 设备栈版本偏离 `requirements-reference.txt`：已数值对齐，是否接受由组长确认
- 范围外：环境脚本 → 3号；源码回退 → 5号；安装文档 → 6号
